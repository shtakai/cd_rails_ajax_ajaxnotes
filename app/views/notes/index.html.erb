<div class="container">
  <% @notes.each do |note| %>
    <div class="well" id="note-<%= note.id %>">
      <%= note.id %>
      <div class="title">
        <%= note.title %>
      </div>
      <form action="/notes/<%= note.id %>" method="POST">
      </form>
      <%= form_tag "/notes/#{note.id}", method: :PATCH, remote: true do%>
        <textarea name="description" cols="30" rows="10" readonly class="description"><%= note.description %></textarea>
        <input type="submit" value="Edit" class="submit">
      <% end %>
      <%= form_tag "/notes/#{note.id}", method: :DELETE, remote: true, class: 'destroy' do%>
        <input type="submit" value="Delete">
      <% end %>
    </div>
  <% end %>
</div>
<div class="container">
  <%= simple_form_for(@note, html: { class: 'form-horizontal' }) do |f| %>
    <%= f.input :title, placeholder: 'Insert note title here..'  %>
    <%= f.submit 'Add Note' %>
  <% end %>

</div>
